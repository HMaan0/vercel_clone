{
  "name": "@repo/db",
  "version": "0.0.0",
  "dependencies": {
    "@prisma/client": "^6.5.0"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma migrate dev --skip-generate"
  }
}
